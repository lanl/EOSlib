#! /usr/bin/env amrita

if( token(amrita:sys::redo) ) then
    execute 'rm' -rf include lib bin
end if
utilize EOSlib::Amrita
fold::amrita { print EOSlib::* tokens
  tokens EOSlib::* -> EOSlib
  foreach var  ( $EOSlib ) split on /\s+/
    echo $var = $$var
  end foreach
}
